# LocateRoom 知识扩展

> 基于当前 OpenSpec 设计，解释每一项技术选型的原因和取舍

---

## 1. WebRTC 是什么

**WebRTC (Web Real-Time Communication)** 是一组浏览器原生支持的 API，允许两个浏览器之间直接建立点对点（P2P）连接，传输音视频或任意数据，无需经过服务器中转。

### 核心组件

```
WebRTC
├── MediaStream API     → 获取摄像头/麦克风（本次不用）
├── RTCPeerConnection  → 建立和管理 P2P 连接
└── RTCDataChannel    → 在连接上传输任意二进制数据 ← 本项目用的就是这个
```

### 为什么用它

| 方案 | 描述 | 缺点 |
|---|---|---|
| HTTP 轮询 | 客户端定时请求服务器拉取他人位置 | 延迟高、浪费带宽 |
| WebSocket 中转 | 所有数据经过服务器转发 | 服务器成本、延迟、违背 P2P 约束 |
| **WebRTC DataChannel** | 浏览器直连，位置数据 P2P 传输 | NAT 穿透可能失败 |

### WebRTC 的局限（为什么不是"零服务器"）

WebRTC 只需要服务器做**信令（Signaling）**——即帮助双方交换"在哪里、怎么找到彼此"的信息。一旦连接建立，数据确实走 P2P。但信令服务器是不可省略的。

```
浏览器 A                    服务器（信令）                    浏览器 B
   │                            │                              │
   │──── offer (自己的IP/端口) ──→│                              │
   │                            │──── offer ───────────────────→│
   │                            │                              │
   │←─── answer (B的IP/端口) ────│                              │
   │                            │                              │
   │←──── ICE candidate ────────│←──── ICE candidate ──────────│
   │                            │                              │
   └─── 直接 P2P DataChannel ─────────────────────────────────→│
        （位置数据走这里，不经过服务器）
```

---

## 2. 信令层：Firebase Realtime DB

### 什么是信令

WebRTC 连接建立前，两个浏览器需要交换：
- **SDP (Session Description Protocol)**：offer/answer，描述媒体能力
- **ICE Candidate**：网络候选地址，用于 NAT 穿透

这些交换需要一个**共同的频道**，这就是信令。Firebase Realtime DB 充当了这个频道。

### Firebase Realtime DB 的数据结构

```json
{
  "rooms": {
    "abc123": {
      "meta": { "createdAt": 1716000000, "hostId": "peer-A" },
      "signals": {
        "peer-A": { "type": "offer", "sdp": "v=0\\r\\no=- ...", "ts": 1716000001 },
        "peer-B": { "type": "answer", "sdp": "v=0\\r\\no=- ...", "ts": 1716000002 }
      },
      "members": {
        "peer-A": { "online": true, "lastSeen": 1716000001 },
        "peer-B": { "online": true, "lastSeen": 1716000001 }
      }
    }
  }
}
```

### 为什么选 Firebase 而非 WebSocket 服务器

| 维度 | Firebase | 自建 WebSocket 服务器 |
|---|---|---|
| 部署成本 | 静态站点，Firebase 免费额度足够 | 需要持续运行的服务器 |
| 掉线检测 | 内置 `onDisconnect` 自动标记离线 | 需要自己实现心跳 |
| 房间元数据 | 自动同步，SDK 成熟 | 需要自己写 CRUD |
| 复杂度 | 前端 SDK 开箱即用 | 需要后端代码 |

**核心取舍**：Firebase 解决了"零后端服务器"的问题，但引入了对 Google 服务的依赖（国内可能不稳定）。

### Firebase 的 onDisconnect

当浏览器与 Firebase 的连接断开时，Firebase 自动执行预设的写入操作：

```javascript
firebase.database().ref(`rooms/${roomId}/members/${myId}`)
  .onDisconnect()
  .update({ online: false });  // 浏览器掉线时自动执行
```

这比手动实现 WebSocket 心跳+服务器检测要简单得多。

---

## 3. NAT 穿透：STUN 和 TURN

### 为什么需要 NAT 穿透

大多数设备不拥有公网 IP，它们的请求经过路由器/NAT 转换后，外界无法直接向它发包。WebRTC 必须解决"怎么找到对方"的问题。

### STUN（Simple Traversal of UDP through NATs）

通过询问公网 STUN 服务器，让设备知道自己的公网 IP:端口。

```
设备 → STUN服务器："我的IP是什么？" → STUN服务器 → 设备："你的公网IP是 X.X.X.X"
```

**局限性**：对称型 NAT（Symmetric NAT）下 STUN 失效。

### TURN（Traversal Using Relays around NAT）

STUN 失败时的终极方案：数据经过中继服务器转发。
- Firebase 只做信令，不做 TURN
- Google 提供公开的 STUN 服务器（`stun:stun.l.google.com:19302`）
- 没有公开免费的 TURN 服务器可用

### 当前设计的风险

当前只用 Google 公开 STUN，没有 TURN fallback。**对称 NAT 环境（企业网络、部分移动网络）下连接可能建立失败**。这是文档中已标注的风险。

---

## 4. Mesh 全互联拓扑

### 什么是 Mesh 拓扑

4 人房间，每个成员都与其他人建立直接连接：

```
    A ─── B
   /│\   /│
  / │ \ / │
 C ─── D

A 连接: B, C, D（3条）
B 连接: A, C, D（3条）
总计: 6 条连接
```

### Mesh vs SFU（Selective Forwarding Unit）

| | Mesh（全互联） | SFU（服务器中转） |
|---|---|---|
| 服务器 | 不需要（信令除外） | 需要，成本高 |
| 带宽（发送方） | O(n)，每人发 n-1 份 | O(1)，每人发一份给服务器 |
| 延迟 | 最低（直连） | 稍高（经过服务器） |
| 适合规模 | ≤ 6 人 | > 6 人 |
| 4 人开销 | 120 bytes × 10Hz × 3 = 3.6KB/s | 不需要 |

### 为什么选 Mesh 而非 SFU

需求明确说 4 人上限。4 人场景下，Mesh 的带宽/延迟优势远大于 SFU 的扩展性优势。SFU 需要额外服务器，违背了"零后端"的部署目标。

**规模扩展限制**：如果需求变成 10 人房间，Mesh 每人需要 9 条连接，管理复杂度大幅上升，此时应该考虑 SFU。这是当前设计的一个边界条件。

### 连接建立顺序问题：peerId 字典序

多人同时加入时，如果每对都双向同时发 offer，会导致冲突。解决方式：

```
peer-A (字典序更小) → 发 offer
peer-B (字典序更大) → 等待 offer，收到后回复 answer
```

通过 peerId 的字典序决定 initiator，保证每对只建立一条连接，避免重复。

---

## 5. RTCDataChannel

### 什么是 DataChannel

DataChannel 是 WebRTC 连接上的双向数据传输通道，有点像 UDP socket（但可以配置为可靠传输）。

```
RTCPeerConnection
    ├── MediaStreamTrack × N  （音视频，本次不用）
    └── RTCDataChannel × N    （数据，本次就用这个）
```

### DataChannel 配置

```javascript
const dc = peerConnection.createDataChannel('position', {
  ordered: true,       // 保证顺序（位置不能乱序，否则跳变）
  maxRetransmits: 3   // 最多重传3次，超过则丢弃
});
```

| 配置 | 含义 | 选择原因 |
|---|---|---|
| `ordered: true` | 保证消息按发送顺序到达 | 位置数据乱序会导致地图上标记跳动 |
| `ordered: false` | 不保证顺序，延迟更低 | 适合语音对讲（容忍丢包） |
| `maxRetransmits: 0` | 纯 UDP，不重传 | 适合实时性要求极高、容忍丢包场景 |
| `maxRetransmits: 3` | 有限重传 | 位置数据偶尔丢一帧可以接受，但不想无限重传造成卡顿 |

### 为什么用 DataChannel 而非 MediaStream

需求要求位置数据走 P2P，DataChannel 正是为此设计的：
- 极低延迟（直接走 WebRTC 底层）
- 灵活二进制格式（8 bytes/帧）
- 浏览器原生支持，不需要编解码器

---

## 6. 位置编码：为什么用 Float32Array（8 bytes）

### 二进制 vs JSON

```
JSON 格式: {"lat": 31.2304, "lng": 121.4737}  →  约 50 字节
Float32Array: Float32Array([31.2304, 121.4737])  →  8 字节
```

### Float32 的精度够吗

地球周长约 40,000 km，Float32 的精度约为：
- 2^(-23) × 360° ≈ 0.000042°（约 4.7 米）

对于"看到朋友在地图上"这个场景，4-5 米的精度完全够用。如果需要更精确（如室内定位），应改用 Float64（16 bytes/帧）。

### 编解码代码

```javascript
// 编码（发送方）
function encodePosition(lat, lng) {
  const buf = new ArrayBuffer(8);
  const view = new Float32Array(buf);
  view[0] = lat;
  view[1] = lng;
  return buf;
}

// 解码（接收方）
function decodePosition(buf) {
  const view = new Float32Array(buf);
  return { lat: view[0], lng: view[1] };
}
```

### 带宽计算

```
4 人房间，每个位置 8 bytes，10Hz：
8 bytes × 10/s × 3 peers = 240 bytes/s ≈ 2 Kbps

即使加上心跳（每 5s 一次，极小开销），总带宽极低。
```

---

## 7. Geolocation API 与 10Hz 的问题

### 浏览器 Geolocation API

```javascript
navigator.geolocation.watchPosition(
  (pos) => { /* 成功回调 */ },
  (err) => { /* 错误回调 */ },
  {
    enableHighAccuracy: true,  // 启用高精度（GPS）
    maximumAge: 0,             // 不缓存，每次都重新获取
    timeout: 10000            // 10秒超时
  }
);
```

### 为什么 10Hz 难以保证

移动端 GPS 更新频率由操作系统决定：
- iOS Safari：约 1-3 Hz（受系统节电策略限制）
- Android Chrome：约 1-5 Hz（取决于设备和 GPS 信号质量）
- 桌面浏览器：可以更高，但通常也达不到 10Hz

`maximumAge: 0` 能让每次获取都是实时位置，但无法让 GPS 更频繁地更新。

**设计取舍**：代码中实现了 100ms 节流（相当于 10Hz 上限），但实际能达到的频率取决于设备 GPS。用 `Date.now()` 检查时间间隔，不超过 100ms 就发：

```javascript
let lastSend = 0;
watchPosition((pos) => {
  const now = Date.now();
  if (now - lastSend >= 100) {  // 100ms = 10Hz 上限
    broadcastPosition(pos.coords.latitude, pos.coords.longitude);
    lastSend = now;
  }
});
```

### 精度模式的选择

| 模式 | 说明 | 适用场景 |
|---|---|---|
| `enableHighAccuracy: true` | GPS（室外）、WiFi/基站（室内） | 户外定位，精度 1-5m |
| `enableHighAccuracy: false` | 网络定位 | 省电，但精度 100m+ |

当前设计用高精度模式，对移动端电池消耗会更大，属于已知的取舍。

---

## 8. 掉线检测与心跳机制

### 为什么需要心跳

WebRTC 连接建立后，浏览器无法感知对方是否掉线（除非 TCP 层通知）。心跳是一种应用层存活检测机制。

### 当前设计：5s 心跳 + 30s 超时

```
发送方：每 5 秒发送 heartbeat
接收方：30 秒没收到任何消息 → 标记对方离线
```

心跳消息内容可以是任意小字符串（如 `"ping"`），开销几乎为零。

### 为什么是 30 秒而非更短

- 太短（如 5s）：网络抖动时容易误判
- 太长（如 60s）：掉线感知延迟大，影响体验
- 30s 是一个平衡点，配合 5s 心跳是工程实践中常见的配置

### onDisconnect 的补充作用

Firebase 的 `onDisconnect` 会在网络断开时自动标记 `online: false`，这与 DataChannel 心跳形成双重保险：
- DataChannel 心跳：检测应用层断线（P2P 连接问题）
- Firebase onDisconnect：检测网络层断线（WiFi 断开等）

两者结合可以更可靠地感知掉线。

---

## 9. 断网重连的复杂性

### 为什么"自动恢复"很复杂

当网络中断后重新连接：
1. Firebase 重连 → 重新订阅房间信号
2. 检测到与某人的 DataChannel 已关闭
3. 重新走 WebRTC 建联流程（offer → answer → ICE → connected）

问题在于：
- 对方可能已离开房间
- 对方可能也在重连，不知道该由谁发起
- ICE 协商可能再次失败

### 当前设计的简化假设

设计文档中"自动重连"没有详细定义，实际上是**尽力恢复**：检测到断线后重新发 offer，能连上就连，连不上就放弃。这在大多数情况下有效，但无法保证 100% 可靠。

**这是需求文档中被低估的部分**，也是实际实现中需要花最多时间的地方之一。

---

## 10. Leaflet 地图

### 为什么用 Leaflet + OSM

| 方案 | 优点 | 缺点 |
|---|---|---|
| Google Maps API | 精度高、覆盖好 | 需要 API key、需要信用卡、付费 |
| Mapbox | 好看、功能强 | 需要 API key、付费 |
| **Leaflet + OSM** | 免费、无需 API key、开源、轻量 | 地图覆盖不如 Google 全 |
| 高德/百度地图 | 国内数据好 | API 复杂、需要申请 |

Leaflet 约 50KB（gzip 后），OSM 街道图层完全免费。对于"朋友在地图上互相可见"这个需求，OSM 精度完全够用。

### 为什么不加分包引入

Leaflet 有多个子模块（marker、polyline、control 等），可以按需引入减少体积。当前设计是引入完整包，原因是：
- 项目规模小，包体积不是瓶颈
- 简化构建复杂度，不需要额外配置

---

## 11. 性能指标分析

### 50fps 渲染帧率

影响帧率的环节：

```
GPS (10Hz) → 位置编码 (微秒级) → DataChannel 发送 (微秒级)
           → 接收解码 → 地图更新 DOM → 浏览器重排/重绘
```

Leaflet 的 `circleMarker` 更新只是改变 DOM 坐标，浏览器渲染开销很小。真正的瓶颈可能在：
- 同时更新多个 marker（4人×每帧1次更新 = 4次 DOM 操作/帧）
- 移动端浏览器 compositor 性能

**50fps 是一个相对宽松的目标**，现代中端设备应该能轻松达到。

### 3 秒首次可见

```
Firebase 写入房间元数据: ~100ms
新成员订阅房间变化: ~100ms（Firebase SDK）
现有成员检测到新成员: ~100ms
新成员向每个现有成员发 offer: N × 100ms
对方回复 answer: N × 100ms
ICE 协商: 1-2s（最不稳定环节）
DataChannel 打开 + 发送初始位置: ~100ms
```

3 秒是一个紧张但可能达到的目标，前提是网络状况良好。如果 ICE 穿透失败（对称 NAT），时间会显著增加。

---

## 12. 技术选型总览与取舍原因

| 选型 | 决定 | 原因 | 代价 |
|---|---|---|---|
| 信令 | Firebase Realtime DB | 零服务器、内置掉线检测、免费额度够用 | 依赖 Google 服务 |
| 位置传输 | DataChannel P2P | 满足需求约束、延迟低、无服务器带宽成本 | NAT 穿透可能失败 |
| 网络拓扑 | Mesh 全互联 | 4人规模下带宽可接受、不需要 SFU 服务器 | 规模超过6人时 O(n²) 连接数爆炸 |
| 位置编码 | Float32Array 8字节 | 相比 JSON 减少 6x 带宽、延迟低 | 需要二进制编解码，比 JSON 调试难 |
| 地图 | Leaflet + OSM | 免费、无需 API key、轻量 | 地图覆盖不如 Google |
| 前端框架 | 原生 JS | 项目规模小、框架复杂度不值得 | 无组件化，大规模后维护困难 |
| DataChannel | ordered, maxRetransmits: 3 | 保证位置顺序、有有限重传 | 不如 unordered 那样可以完全丢弃延迟 |

---

## 13. 未解决的技术债务

以下问题在实际实现时需要特别注意：

### 13.1 信令竞争条件

当两个用户同时加入空房间，两人互相看不到对方，无法建立连接。解决方案：先加入房间的人建立连接，后加入的人检测到已有连接后主动发起。

### 13.2 对称 NAT 穿透失败

当前仅用 Google 公开 STUN，对称 NAT 下 WebRTC 无法建立。需要 TURN 服务器（如 Twilio 的免费 TURN），但这会增加依赖和成本。

### 13.3 Firebase 安全规则

开发阶段开放读写，生产环境需要配置 Firebase Realtime DB 安全规则，防止未授权访问房间数据。

### 13.4 iOS Safari 后台挂起

iOS Safari 进入后台后 JavaScript 被暂停，GPS 停止上报。对方看到位置"冻结"。解决方案：检测 `visibilitychange` 事件，进入后台时通知对方自己已离线。