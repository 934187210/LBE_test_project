# LocateRoom 需求存疑点

## 技术层面的问题

### 1. "数据必须走 WebRTC DataChannel P2P 传输" 存在内部矛盾

WebRTC P2P 并不意味着不需要服务器：
- 仍需要 **信令服务器**（WebSocket）来建立连接
- NAT 穿透失败时必须用 **TURN 中继服务器**，此时数据并非真正 P2P
- 4 人房间是全 mesh 拓扑，需要建立 6 条 P2P 连接——连接数随人数增长是 O(n²)

相比之下，用一个普通 WebSocket 服务器广播位置数据，实现更简单、延迟更低、可靠性更高。要求强制 P2P 但不解释原因，技术收益存疑。

### 2. "每人 10 Hz 上报" 在移动端不可靠

`navigator.geolocation.watchPosition` 的更新频率由操作系统决定，移动端 GPS 通常只有 **1–5 Hz**，Safari 上尤为明显。可以用 `maximumAge: 0` 尽量加快，但无法保证 10 Hz。

### 3. "加入房间到首次看到他人位置 ≤ 3 秒" 与 P2P 约束冲突

WebRTC ICE 协商本身在移动网络下就可能需要 1–3 秒，再加上信令往返，3 秒是比较紧张的指标。如果 STUN 穿透失败降级到 TURN，时间会更长。

### 4. "断网重连自动恢复" 被严重低估

WebRTC 断网重连需要重新走完整的 信令 → ICE → DTLS 流程，对端还需要配合响应。这不是几行代码能稳定实现的，是个独立的复杂工程问题。

---

## 产品层面的问题

### 5. 时间预期不匹配

文档说"建议实际投入 4–6 小时"，但要稳定实现：
- 信令服务器
- WebRTC mesh 管理
- 地图渲染
- 断网重连
- 移动端兼容

实际上是 **20–40 小时**的工作量。考察目的可能在于观察候选人如何取舍，但如果候选人按字面理解，会面临无法完成的窘境。

---

## 总结

| 问题 | 严重程度 |
|---|---|
| P2P 强制约束实际仍需服务器 | 中（概念模糊） |
| 移动端 10 Hz GPS 无法保证 | 高（功能无法实现） |
| 3 秒首见与 WebRTC 建连时间冲突 | 中（指标过紧） |
| 断网重连复杂度被低估 | 高（容易成坑） |
| 时间预期严重不足 | 高（考察意图不明确） |

**建议**：把 P2P 约束理解为"位置数据走 DataChannel，而非 HTTP 轮询"，优先实现核心功能，在 README 里写清楚哪些地方做了取舍及原因——这才是这份作业真正考察的东西。